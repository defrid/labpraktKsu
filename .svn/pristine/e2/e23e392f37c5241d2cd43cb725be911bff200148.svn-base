$.extend(true, window, {
    "Slick": {
        "Validators": {
            "Function": FunctionValidator
        }
    }
});

function FunctionValidator(options) {
    var validationFunction = new Function('value', 'row', options.function);

    this.validate = function(value, row){
        return validationFunction(value, row);
    }
};