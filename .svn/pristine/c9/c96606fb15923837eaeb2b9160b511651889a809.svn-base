(function (window, angular) {
    'use strict';

    angular.module('edm')
        .factory('User', function ($resource) {

            var User;

            User = $resource('/users');

            return User;
        });
}(window, window.angular));
